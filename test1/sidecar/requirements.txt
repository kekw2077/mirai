# EVS sidecar — voice/ML brain (STT, VAD, TTS, intent).
# Python 3.10–3.12 recommended on Windows.
faster-whisper>=1.0.0
# GigaAM-v3 STT (offline transducer), streaming denoise (GTCRN/DeepFilterNet)
# and Piper TTS (OfflineTts on VITS voices) all run on sherpa-onnx.
sherpa-onnx>=1.13.3
sounddevice>=0.4.6
webrtcvad>=2.0.10
pyttsx3>=2.90
rapidfuzz>=3.0.0
websockets>=12.0
numpy>=1.24
# GPU detection + VRAM polling for the CPU/GPU selector and game mode (TZ2
# blocks 6/7). Pure-Python NVML binding; absent an NVIDIA GPU it just reports
# "unavailable" and the features stay hidden/off.
nvidia-ml-py>=12.0.0
# Reading the synthesized wav for chunked playback with live level emission
# (tts.level events driving the app's voice visualizations).
soundfile>=0.12.0
# webrtcvad 2.0.10 imports pkg_resources, removed in setuptools>=81. Pin below
# that so the import keeps working (frozen exe and dev venv alike).
setuptools<81
# Per-application volume via Windows Core Audio sessions (new-features Ф2:
# "громкость на 30"). Pure-Python over comtypes (already pulled in by pyttsx3).
pycaw>=20240210
